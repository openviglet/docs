# Turing ES — Documentation Update Plan (RETIRED)

> **Working/planning artifact — not a published page.** The leading `_` keeps
> Docusaurus from including this file in the build or sidebar.

---

## Retired — 2026-06-30

This planning document has been **retired**. It scoped the **first** documentation
round, **Block U** (T411–T423, parity through T410), which shipped, and was
superseded by the **second** round, **Block AE** (T524–T538, parity through T523 +
a didactic "from-the-beginning" layer), which also shipped.

**The living coverage gate is now [`COVERAGE.md`](./COVERAGE.md).** It maps every
shipped Turing `CHANGELOG` block/feature to its documentation page (or an explicit
"deliberately internal" row), so an orphaned subsystem is a visible red row rather
than a silent omission. Maintain that file when a new block ships; there is no
separate plan to keep in sync.

- Round 1 design rationale: Turing `docs/IMPROVEMENTS.md` (Block U section, removed on ship).
- Round 2 design rationale: Turing `docs/IMPROVEMENTS.md` §XXIX (Block AE).
- Authoritative "what shipped": Turing `docs/CHANGELOG.md`.
