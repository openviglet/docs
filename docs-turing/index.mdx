---
sidebar_position: 1
title: Turing ES
description: Viglet Turing ES — Enterprise Search with Semantic Navigation and Generative AI
slug: /
---

import Link from '@docusaurus/Link';

export const VERSION = "2026.1";
export const PDF_FILENAME = `turing-es-${VERSION}-documentation.pdf`;

export const Pill = ({ children, color = "var(--color-brand)" }) => (
  <span style={{
    display: "inline-flex", alignItems: "center", gap: "6px",
    background: "var(--color-brand-bg)", color: color,
    border: "1px solid var(--color-brand-border)",
    borderRadius: "999px", padding: "2px 12px",
    fontSize: "0.75rem", fontWeight: 600, letterSpacing: "0.02em",
  }}>
    {children}
  </span>
);

export const FeatureCard = ({ icon, title, children }) => (
  <div style={{
    background: "var(--color-card)", border: "1px solid var(--color-border)",
    borderRadius: "12px", padding: "1.25rem 1.5rem",
    display: "flex", flexDirection: "column", gap: "6px",
  }}>
    <div style={{ fontSize: "1.75rem", lineHeight: 1 }}>{icon}</div>
    <div style={{ fontWeight: 700, fontSize: "0.9375rem", color: "var(--color-foreground)" }}>{title}</div>
    <div style={{ fontSize: "0.875rem", color: "var(--color-muted-foreground)", lineHeight: 1.6 }}>{children}</div>
  </div>
);

export const NavCard = ({ icon, title, description, href }) => (
  <Link to={href} style={{ textDecoration: "none" }}>
    <div style={{
      background: "var(--color-card)", border: "1px solid var(--color-border)",
      borderRadius: "10px", padding: "1rem 1.25rem",
      display: "flex", gap: "12px", alignItems: "flex-start",
      transition: "box-shadow 0.15s, transform 0.15s",
      cursor: "pointer",
    }}
    onMouseEnter={e => { e.currentTarget.style.transform = "translateY(-2px)"; e.currentTarget.style.boxShadow = "0 4px 20px rgba(0,0,0,0.08)"; }}
    onMouseLeave={e => { e.currentTarget.style.transform = ""; e.currentTarget.style.boxShadow = ""; }}
    >
      <div style={{
        width: "36px", height: "36px", borderRadius: "8px", flexShrink: 0,
        background: "var(--color-brand-bg)", display: "flex", alignItems: "center",
        justifyContent: "center", fontSize: "1.1rem",
      }}>{icon}</div>
      <div>
        <div style={{ fontWeight: 600, fontSize: "0.9rem", color: "var(--color-foreground)", marginBottom: "2px" }}>{title}</div>
        <div style={{ fontSize: "0.8125rem", color: "var(--color-muted-foreground)", lineHeight: 1.5 }}>{description}</div>
      </div>
    </div>
  </Link>
);

export const SectionLabel = ({ children }) => (
  <div style={{ display: "flex", alignItems: "center", gap: "10px", margin: "2.5rem 0 1rem" }}>
    <div style={{ height: "1px", flex: 1, background: "var(--color-border)" }} />
    <span style={{
      fontSize: "0.7rem", fontWeight: 700, letterSpacing: "0.1em",
      textTransform: "uppercase", color: "var(--color-muted-foreground)", whiteSpace: "nowrap",
    }}>{children}</span>
    <div style={{ height: "1px", flex: 1, background: "var(--color-border)" }} />
  </div>
);

{/* ── Header ──────────────────────────────────────────────────────────────── */}

<div style={{ display: "flex", alignItems: "center", gap: "10px", marginBottom: "1rem", flexWrap: "wrap" }}>
  <Pill>v{VERSION}</Pill>
  <Pill>Open Source</Pill>
  <Pill>Apache 2.0</Pill>
</div>

# Viglet Turing ES

**Viglet Turing ES** is an open-source enterprise search platform combining **Semantic Navigation**, **content integration**, and **Generative AI**. Index content from any source, deliver faceted search experiences, and deploy conversational AI grounded in your own data.

{/* ── PDF Download Banner ─────────────────────────────────────────────────── */}

<div style={{
  display: "flex", alignItems: "center", justifyContent: "space-between",
  flexWrap: "wrap", gap: "12px",
  background: "var(--color-accent, var(--color-brand-bg))",
  border: "1px solid var(--color-brand-border)",
  borderLeft: "4px solid var(--color-brand)",
  borderRadius: "10px", padding: "1rem 1.25rem",
  margin: "1.75rem 0",
}}>
  <div style={{ display: "flex", alignItems: "center", gap: "12px", minWidth: 0 }}>
    <div style={{
      width: "38px", height: "38px", borderRadius: "8px", flexShrink: 0,
      background: "var(--color-brand)", display: "flex", alignItems: "center",
      justifyContent: "center", fontSize: "1.2rem",
    }}>📄</div>
    <div style={{ minWidth: 0 }}>
      <div style={{ fontWeight: 700, fontSize: "0.875rem", color: "var(--color-foreground)" }}>
        Complete Documentation — v{VERSION}
      </div>
      <div style={{ fontSize: "0.8rem", color: "var(--color-muted-foreground)", marginTop: "2px" }}>
        All guides, API reference and configuration in a single portable PDF
      </div>
    </div>
  </div>
  <a
    href={`/${PDF_FILENAME}`}
    target="_blank"
    rel="noopener noreferrer"
    style={{
      display: "inline-flex", alignItems: "center", gap: "5px",
      background: "var(--color-brand)", color: "#fff",
      borderRadius: "7px", padding: "6px 14px",
      fontWeight: 600, fontSize: "0.8rem", textDecoration: "none",
      whiteSpace: "nowrap", transition: "opacity 0.15s",
      flexShrink: 0,
    }}
    onMouseEnter={e => e.currentTarget.style.opacity = "0.85"}
    onMouseLeave={e => e.currentTarget.style.opacity = "1"}
  >
    ⬇ Download PDF
  </a>
</div>

{/* ── Feature highlights ──────────────────────────────────────────────────── */}

<SectionLabel>Key Capabilities</SectionLabel>

<div style={{
  display: "grid", gap: "12px",
  gridTemplateColumns: "repeat(auto-fit, minmax(220px, 1fr))",
  marginBottom: "0.5rem",
}}>
  <FeatureCard icon="🔍" title="Semantic Navigation">
    Faceted search, autocomplete, spotlights, targeting rules, result ranking and per-site AI insights.
  </FeatureCard>
  <FeatureCard icon="🤖" title="Generative AI">
    RAG over your indexed content, LLM instances, AI agents, tool calling and MCP server integration.
  </FeatureCard>
  <FeatureCard icon="🔌" title="Content Integration">
    REST API connectors, AEM, web crawler, delta tracking, merge providers and indexing rules.
  </FeatureCard>
  <FeatureCard icon="🔒" title="Security">
    Native session auth, REST API key tokens and production-grade Keycloak OAuth2/OIDC SSO.
  </FeatureCard>
</div>

{/* ── Quick Start ─────────────────────────────────────────────────────────── */}

<SectionLabel>Quick Start</SectionLabel>

<div style={{
  display: "grid", gap: "10px",
  gridTemplateColumns: "repeat(auto-fit, minmax(180px, 1fr))",
  marginBottom: "0.5rem",
}}>
  {[
    { step: "01", title: "Install", desc: "Docker, standalone JAR or from source", href: "/turing/installation-guide" },
    { step: "02", title: "Configure", desc: "application.yaml properties reference", href: "/turing/configuration-reference" },
    { step: "03", title: "Understand", desc: "SN Sites, connectors and the data model", href: "/turing/getting-started/core-concepts" },
    { step: "04", title: "Search", desc: "REST API, search params and response format", href: "/turing/rest-api" },
  ].map(({ step, title, desc, href }) => (
    <Link key={step} to={href} style={{ textDecoration: "none" }}>
      <div style={{
        background: "var(--color-card)", border: "1px solid var(--color-border)",
        borderRadius: "10px", padding: "1rem 1.25rem",
        transition: "box-shadow 0.15s, transform 0.15s",
      }}
      onMouseEnter={e => { e.currentTarget.style.transform = "translateY(-2px)"; e.currentTarget.style.boxShadow = "0 4px 20px rgba(0,0,0,0.08)"; }}
      onMouseLeave={e => { e.currentTarget.style.transform = ""; e.currentTarget.style.boxShadow = ""; }}
      >
        <div style={{ fontSize: "0.7rem", fontWeight: 800, letterSpacing: "0.1em", color: "var(--color-brand)", marginBottom: "6px" }}>
          STEP {step}
        </div>
        <div style={{ fontWeight: 700, fontSize: "0.9375rem", color: "var(--color-foreground)", marginBottom: "4px" }}>{title}</div>
        <div style={{ fontSize: "0.8125rem", color: "var(--color-muted-foreground)", lineHeight: 1.5 }}>{desc}</div>
      </div>
    </Link>
  ))}
</div>

{/* ── Navigation cards ────────────────────────────────────────────────────── */}

<SectionLabel>Administration & Development</SectionLabel>

<div style={{ display: "grid", gap: "10px", gridTemplateColumns: "repeat(auto-fit, minmax(260px, 1fr))" }}>
  <NavCard icon="⚙️" title="Administration Guide" description="Users, roles, API tokens, global settings and system information" href="/turing/administration-guide" />
  <NavCard icon="🛠️" title="Developer Guide" description="Tech stack, dev environment, Java SDK and contribution guide" href="/turing/developer-guide" />
  <NavCard icon="📡" title="REST API Reference" description="Search, autocomplete, spell check, GenAI chat and token usage endpoints" href="/turing/rest-api" />
</div>

<SectionLabel>Enterprise Search</SectionLabel>

<div style={{ display: "grid", gap: "10px", gridTemplateColumns: "repeat(auto-fit, minmax(260px, 1fr))" }}>
  <NavCard icon="🗄️" title="Search Engine" description="Manage Solr, Elasticsearch and Lucene backends, cores and monitoring" href="/turing/search-engine" />
  <NavCard icon="🧭" title="Semantic Navigation" description="Configure SN Sites — fields, facets, spotlights, targeting rules, result ranking and GenAI" href="/turing/semantic-navigation" />
  <NavCard icon="🔗" title="Integration" description="Connect AEM and web crawler sources with indexing rules and monitoring" href="/turing/integration" />
</div>

<SectionLabel>Generative AI</SectionLabel>

<div style={{ display: "grid", gap: "10px", gridTemplateColumns: "repeat(auto-fit, minmax(260px, 1fr))" }}>
  <NavCard icon="🧠" title="GenAI & LLM Configuration" description="RAG architecture, embedding pipeline and core GenAI concepts" href="/turing/genai-llm" />
  <NavCard icon="📖" title="What is RAG?" description="How LLMs, embedding models and vector stores work together to ground AI in your content" href="/turing/rag" />
  <NavCard icon="⚡" title="LLM Instances" description="Configure provider connections — vendors, models, API keys and capabilities" href="/turing/llm-instances" />
  <NavCard icon="🗃️" title="Embedding Stores" description="ChromaDB, PgVector and Milvus vector database backends" href="/turing/embedding-stores" />
  <NavCard icon="🧬" title="Embedding Models" description="Provider support, model selection and local transformers configuration" href="/turing/embedding-models" />
  <NavCard icon="🗂️" title="Assets (Knowledge Base)" description="File manager backed by MinIO — upload, organise and train the RAG knowledge base" href="/turing/assets" />
  <NavCard icon="🔧" title="Tool Calling" description="27 native tools across 7 categories available to AI Agents" href="/turing/tool-calling" />
  <NavCard icon="🌐" title="MCP Servers" description="Extend agents with external tools via the Model Context Protocol" href="/turing/mcp-servers" />
  <NavCard icon="🤖" title="AI Agents" description="Compose and deploy purpose-built AI assistants with custom tools and personas" href="/turing/ai-agents" />
  <NavCard icon="💬" title="Chat" description="Chat interface — direct LLM, Semantic Navigation and AI Agent views" href="/turing/chat" />
  <NavCard icon="📊" title="Token Usage" description="Monitor and analyse LLM token consumption by model and day" href="/turing/token-usage" />
</div>

<SectionLabel>Technical Reference</SectionLabel>

<div style={{ display: "grid", gap: "10px", gridTemplateColumns: "repeat(auto-fit, minmax(260px, 1fr))" }}>
  <NavCard icon="🏗️" title="Architecture Overview" description="Component diagram, indexing and search flows, deployment topologies" href="/turing/architecture-overview" />
  <NavCard icon="🔑" title="Authentication" description="Native session login and REST API Key authentication" href="/turing/security-authentication" />
  <NavCard icon="🛡️" title="Security & Keycloak" description="Full OAuth2/OIDC production setup with Keycloak, Apache proxy and SSL" href="/turing/security-keycloak" />
</div>

<SectionLabel>Other</SectionLabel>

<div style={{ display: "grid", gap: "10px", gridTemplateColumns: "repeat(auto-fit, minmax(260px, 1fr))" }}>
  <NavCard icon="☕" title="Javadoc" description="Java API Reference — full source-level documentation" href="https://turing.viglet.com/latest/javadoc/" />
</div>
