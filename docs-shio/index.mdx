---
sidebar_position: 1
title: Shio CMS
description: "Viglet Shio CMS: an agent-native headless CMS. A coding agent builds the site over MCP, a human curates it, and the Content Delivery API serves it."
slug: /
---

import Link from '@docusaurus/Link';

export const VERSION = "2026.3";

export const Pill = ({ children, color = "var(--color-brand)" }) => (
  <span style={{
    display: "inline-flex", alignItems: "center", gap: "6px",
    background: "var(--color-brand-bg)", color: color,
    border: "1px solid var(--color-brand-border)",
    borderRadius: "999px", padding: "2px 12px",
    fontSize: "0.75rem", fontWeight: 600, letterSpacing: "0.02em",
  }}>
    {children}
  </span>
);

export const FeatureCard = ({ icon, title, children }) => (
  <div style={{
    background: "var(--color-card)", border: "1px solid var(--color-border)",
    borderRadius: "12px", padding: "1.25rem 1.5rem",
    display: "flex", flexDirection: "column", gap: "6px",
  }}>
    <div style={{ fontSize: "1.75rem", lineHeight: 1 }}>{icon}</div>
    <div style={{ fontWeight: 700, fontSize: "0.9375rem", color: "var(--color-foreground)" }}>{title}</div>
    <div style={{ fontSize: "0.875rem", color: "var(--color-muted-foreground)", lineHeight: 1.6 }}>{children}</div>
  </div>
);

export const NavCard = ({ icon, title, description, href }) => (
  <Link to={href} style={{ textDecoration: "none" }}>
    <div style={{
      background: "var(--color-card)", border: "1px solid var(--color-border)",
      borderRadius: "10px", padding: "1rem 1.25rem",
      display: "flex", gap: "12px", alignItems: "flex-start",
      transition: "box-shadow 0.15s, transform 0.15s",
      cursor: "pointer",
    }}
    onMouseEnter={e => { e.currentTarget.style.transform = "translateY(-2px)"; e.currentTarget.style.boxShadow = "0 4px 20px rgba(0,0,0,0.08)"; }}
    onMouseLeave={e => { e.currentTarget.style.transform = ""; e.currentTarget.style.boxShadow = ""; }}
    >
      <div style={{
        width: "36px", height: "36px", borderRadius: "8px", flexShrink: 0,
        background: "var(--color-brand-bg)", display: "flex", alignItems: "center",
        justifyContent: "center", fontSize: "1.1rem",
      }}>{icon}</div>
      <div>
        <div style={{ fontWeight: 600, fontSize: "0.9rem", color: "var(--color-foreground)", marginBottom: "2px" }}>{title}</div>
        <div style={{ fontSize: "0.8125rem", color: "var(--color-muted-foreground)", lineHeight: 1.5 }}>{description}</div>
      </div>
    </div>
  </Link>
);

export const SectionLabel = ({ children }) => (
  <div style={{ display: "flex", alignItems: "center", gap: "10px", margin: "2.5rem 0 1rem" }}>
    <div style={{ height: "1px", flex: 1, background: "var(--color-border)" }} />
    <span style={{
      fontSize: "0.7rem", fontWeight: 700, letterSpacing: "0.1em",
      textTransform: "uppercase", color: "var(--color-muted-foreground)", whiteSpace: "nowrap",
    }}>{children}</span>
    <div style={{ height: "1px", flex: 1, background: "var(--color-border)" }} />
  </div>
);

{/* -- Header -- */}

<div style={{ display: "flex", alignItems: "center", gap: "10px", marginBottom: "1rem", flexWrap: "wrap" }}>
  <Pill>v{VERSION}</Pill>
  <Pill>Open Source</Pill>
  <Pill>GPL v3</Pill>
  <Pill>Headless</Pill>
  <Pill>MCP</Pill>
</div>

# Viglet Shio CMS

**Viglet Shio CMS** is an open-source headless CMS built for a different operator. **A
coding agent builds the site**: over MCP, a REST surface designed for one call instead
of a session, or content projected to files on disk. **A human curates it**: 
approving, correcting and publishing in the console or straight on the rendered page.
**The Content Delivery API serves it**: REST and GraphQL, with a TypeScript client,
React hooks and a Next.js starter.

Shio hosts **no model and no prompts**. It is the substrate your agent drives, running
on your own infrastructure, and what it competes on is the quality of that protocol:
discoverable, deterministic, verifiable, and cheap in tokens.

{/* -- Feature highlights -- */}

<SectionLabel>What makes it different</SectionLabel>

<div style={{
  display: "grid", gap: "12px",
  gridTemplateColumns: "repeat(auto-fit, minmax(220px, 1fr))",
  marginBottom: "0.5rem",
}}>
  <FeatureCard icon="🧭" title="One call, not a session">
    The instance describes itself: a manifest of every capability, plus a context pack of
    the site as it stands. An agent does not have to explore before it can write.
  </FeatureCard>
  <FeatureCard icon="📁" title="Content is also files">
    Project a site to disk and author it with the cheapest tools an agent has, edit,
    write, grep, then push it back, with a per-field three-way merge so a curator's own
    edit is never overwritten.
  </FeatureCard>
  <FeatureCard icon="🛡️" title="Nothing surprises the human">
    Agent writes land as drafts, plans can run dry first, destructive operations need a
    confirm token naming what they remove, and a delete is a trash you can undo.
  </FeatureCard>
  <FeatureCard icon="📐" title="Addressed, not id-shuffled">
    Content is reached by path, <code>post:mysite/about</code>,
    <code>folder:mysite/blog</code>, so no UUID has to survive between two calls, and
    the same write twice is the same result.
  </FeatureCard>
  <FeatureCard icon="✅" title="Proof without a screenshot">
    A content lint, a route check and a render digest give an agent cheap textual
    evidence that what it built actually works, instead of a human round trip.
  </FeatureCard>
  <FeatureCard icon="📉" title="Tokens are a budget">
    Responses carry measured size budgets and a benchmark runs in CI, so a token
    regression fails the build the same way a broken test does.
  </FeatureCard>
</div>

{/* -- Quick Start -- */}

<SectionLabel>Quick Start</SectionLabel>

<div style={{
  display: "grid", gap: "10px",
  gridTemplateColumns: "repeat(auto-fit, minmax(180px, 1fr))",
  marginBottom: "0.5rem",
}}>
  {[
    { step: "01", title: "Run it", desc: "Docker, standalone JAR or from source", href: "/shio/installation-guide" },
    { step: "02", title: "Point an agent at it", desc: "MCP, the agent surface, content as files, the CLI", href: "/shio/category/agent-builds-it" },
    { step: "03", title: "Curate what it built", desc: "The console, drafts, publishing, the Universal Editor", href: "/shio/category/human-curates-it" },
    { step: "04", title: "Serve it", desc: "The CDA, GraphQL, the TypeScript client and the Next.js starter", href: "/shio/category/cda-delivers-it" },
  ].map(({ step, title, desc, href }) => (
    <Link key={step} to={href} style={{ textDecoration: "none" }}>
      <div style={{
        background: "var(--color-card)", border: "1px solid var(--color-border)",
        borderRadius: "10px", padding: "1rem 1.25rem",
        transition: "box-shadow 0.15s, transform 0.15s",
      }}
      onMouseEnter={e => { e.currentTarget.style.transform = "translateY(-2px)"; e.currentTarget.style.boxShadow = "0 4px 20px rgba(0,0,0,0.08)"; }}
      onMouseLeave={e => { e.currentTarget.style.transform = ""; e.currentTarget.style.boxShadow = ""; }}
      >
        <div style={{ fontSize: "0.7rem", fontWeight: 800, letterSpacing: "0.1em", color: "var(--color-brand)", marginBottom: "6px" }}>
          STEP {step}
        </div>
        <div style={{ fontWeight: 700, fontSize: "0.9375rem", color: "var(--color-foreground)", marginBottom: "4px" }}>{title}</div>
        <div style={{ fontSize: "0.8125rem", color: "var(--color-muted-foreground)", lineHeight: 1.5 }}>{desc}</div>
      </div>
    </Link>
  ))}
</div>

{/* -- The four hubs -- */}

<SectionLabel>Who does which job</SectionLabel>

<div style={{ display: "grid", gap: "10px", gridTemplateColumns: "repeat(auto-fit, minmax(260px, 1fr))" }}>
  <NavCard icon="🤖" title="The agent builds it" description="MCP, the agent surface, content as files, the CLI, blueprints and the verification loop" href="/shio/category/agent-builds-it" />
  <NavCard icon="👤" title="The human curates it" description="The console, drafts and publishing, the Universal Editor, preview and the trash" href="/shio/category/human-curates-it" />
  <NavCard icon="📡" title="The CDA delivers it" description="The read contract, GraphQL, the TypeScript client, React hooks and the Next.js starter" href="/shio/category/cda-delivers-it" />
  <NavCard icon="⚙️" title="Run it" description="Install, configure, multi-tenancy, search indexing and security on your own infrastructure" href="/shio/category/run-it" />
</div>

<SectionLabel>Learn the model</SectionLabel>

<div style={{ display: "grid", gap: "10px", gridTemplateColumns: "repeat(auto-fit, minmax(260px, 1fr))" }}>
  <NavCard icon="🧩" title="Core Concepts" description="Sites, Folders, Posts, Post Types and the publishing workflow" href="/shio/getting-started/core-concepts" />
  <NavCard icon="📋" title="Content Modeling" description="Post Types, custom fields, publishing workflow and content relationships" href="/shio/content-modeling" />
  <NavCard icon="🎨" title="Pages, Layouts & Regions" description="Handlebars templates, Themes, design tokens, helpers, preview and public delivery" href="/shio/website-development" />
  <NavCard icon="🏗️" title="Architecture Overview" description="Component diagram, request flow, technology stack and deployment topologies" href="/shio/architecture-overview" />
</div>

<SectionLabel>Reference</SectionLabel>

<div style={{ display: "grid", gap: "10px", gridTemplateColumns: "repeat(auto-fit, minmax(260px, 1fr))" }}>
  <NavCard icon="📡" title="REST API Reference" description="Post, folder, site, search, user, and widget endpoints" href="/shio/rest-api" />
  <NavCard icon="📊" title="GraphQL" description="Query content using GraphQL with the built-in GraphiQL console" href="/shio/graphql" />
  <NavCard icon="⚙️" title="Administration Guide" description="Users, groups, roles, permissions, site properties and system settings" href="/shio/administration-guide" />
  <NavCard icon="🔑" title="Security" description="Authentication, authorization, user and group management" href="/shio/security" />
  <NavCard icon="📦" title="Import & Export" description="Exchange providers and Shio package backup/restore" href="/shio/import-export" />
  <NavCard icon="🛠️" title="Developer Guide" description="Tech stack, dev environment, build from source and contribution guide" href="/shio/developer-guide" />
</div>
